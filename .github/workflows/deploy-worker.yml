name: Build & Deploy Animal Cup Online
# 任意分支推送都会触发
on:
  push:
    branches: ['*']
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Install dependencies
        run: pnpm install

      - name: Build worker（带DO注入参数）
        env:
          ALLOWED_ORIGINS: https://animal-cup-online.275969940.workers.dev,https://game.zoo-cup.com
          NEXT_PUBLIC_ONLINE_SERVICE_URL: https://game.zoo-cup.com
        run: pnpm deploy:online

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy -c wrangler-online.toml
